import React from "react";
import { Typography } from "@mui/material";
import PageWrapper from "@/src/PageWrapper";

export default function Register() {
  return (
    <PageWrapper sx={{
      width: '100%',
      height: '100%',
      backgroundColor: 'lightblue'
    }}>
      <Typography>Hi Register</Typography>
    </PageWrapper>
  );
}
